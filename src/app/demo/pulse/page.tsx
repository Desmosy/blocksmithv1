import { PulseDemo } from "@/components/demo/PulseDemo";

export const metadata = {
  title: "Pulse demo — @blocksmith/acme-ui-kit",
  description: "Import generated design tokens and components from scan markdown.",
};

export default function PulseDemoPage() {
  return <PulseDemo />;
}
